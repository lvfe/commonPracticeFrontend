import { createAction } from '@ngrx/store';

export const changeTitle = createAction('[SubTitle Component] Change Title');

export const loadContent = createAction('[SubTitle Component] Load Content');